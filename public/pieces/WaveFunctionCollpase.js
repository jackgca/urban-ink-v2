class Block {
	constructor(x, y, blockType) {
		this.x = x;
		this.y = y;
		this.blockType = blockType;
	}

	getNeighbours() {

	}
}

class Blocks {
	constructor(numRows, numCols, blockSize) {
		this.numRows = numRows;
		this.numCols = numCols;
		this.blockSize = blockSize;
	}
}

function WaveFunction() {

}